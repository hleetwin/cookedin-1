'use strict';

describe('Controller: JobCtrl', function () {

  // load the controller's module
  beforeEach(module('cookedinApp'));

  var MainCtrl,
      scope,
      $httpBackend;

  // Initialize the controller and a mock scope
  });
